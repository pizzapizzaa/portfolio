---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
---

<Layout title="Verity Nature - Case Study">
  <Header basePath="/" />

  <main class="pt-24">

    <!-- Hero -->
    <section class="relative min-h-screen flex items-center py-24" style="background-image: url('/assets/projects/verity-mrv-01.jpg'); background-size: cover; background-position: center;">
      <div class="absolute inset-0" style="background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.85));"></div>
      <div class="relative z-10" style="max-width: 860px; margin-left: auto; margin-right: auto; width: 100%; padding-left: 24px; padding-right: 24px;">
        <h1 class="text-[64px] md:text-[100px] leading-none font-heading font-bold text-white mb-6">Verity Nature</h1>
        <div class="flex flex-wrap gap-3 text-[15px]" style="color: rgb(255,184,0);">
          <span>Role: Lead Product Designer</span>
          <span style="opacity:0.4;">|</span>
          <span>Timeline: X Months</span>
          <span style="opacity:0.4;">|</span>
          <span>Status: Shipped / MVP</span>
        </div>
      </div>
      <div class="scroll-cue">
        <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 256 256"><path d="M213.66,101.66l-80,80a8,8,0,0,1-11.32,0l-80-80A8,8,0,0,1,53.66,90.34L128,164.69l74.34-74.35a8,8,0,0,1,11.32,11.32Z" fill="currentColor"/></svg>
      </div>
    </section>

    <!-- Case Study Body -->
    <div class="case-study-body reveal-hidden">

      <section class="case-section">
        <h2 class="case-section-title">1. The TL;DR <span class="case-section-sub">(Executive Summary)</span></h2>
        <div class="case-field">
          <h3 class="case-field-label">The Purpose</h3>
          <p class="case-field-value">Why does this product exist? What is the broader goal or mission it serves?</p>
        </div>
        <div class="case-field">
          <h3 class="case-field-label">The Problem</h3>
          <p class="case-field-value">What was the single biggest pain point for the user or the business?</p>
        </div>
        <div class="case-field">
          <h3 class="case-field-label">The Solution</h3>
          <p class="case-field-value">One sentence describing the core product/feature delivered.</p>
        </div>
        <div class="case-field">
          <h3 class="case-field-label">The Impact</h3>
          <p class="case-field-value">The &ldquo;Hero&rdquo; Metric. (e.g., +25% conversion, -40% support tickets, or $1M in revenue).</p>
        </div>
      </section>

      <section class="case-section">
        <h2 class="case-section-title">2. Strategy &amp; Discovery</h2>
        <div class="case-field">
          <h3 class="case-field-label">Insight</h3>
          <p class="case-field-value">What did the data or user research tell you that others missed?</p>
        </div>
        <div class="case-field">
          <h3 class="case-field-label">Stakeholders</h3>
          <p class="case-field-value">How did you align the Product, Engineering, and Marketing teams?</p>
        </div>
        <div class="case-field">
          <h3 class="case-field-label">Constraint</h3>
          <p class="case-field-value">What was the main &ldquo;blocker&rdquo; (Technical debt, tight deadline, or budget)?</p>
        </div>
      </section>

      <section class="case-section">
        <h2 class="case-section-title">3. Execution <span class="case-section-sub">(The &ldquo;How&rdquo;)</span></h2>
        <div class="case-field">
          <h3 class="case-field-label">Key Decision</h3>
          <p class="case-field-value">&ldquo;We chose [Option A] over [Option B] because of [Data/Logic].&rdquo;</p>
        </div>
        <div class="case-field">
          <h3 class="case-field-label">Design Leadership</h3>
          <p class="case-field-value">Mention the Design System, components, or accessibility standards you enforced.</p>
        </div>
        <div class="case-field">
          <h3 class="case-field-label">Visual Highlights</h3>
          <!-- Gallery -->
          <div class="gallery-wrap">
            <div class="gallery-track-wrap">
              <div class="gallery-track" id="galleryTrack">
                <img class="gallery-img" src="/assets/projects/verity-mrv-01.jpg" alt="Project screen 1" />
                <img class="gallery-img" src="/assets/projects/verity-mrv-01.jpg" alt="Project screen 2" />
              </div>
            </div>
            <div class="gallery-controls">
              <button class="gallery-btn" id="galleryPrev" aria-label="Previous">&#8592; Back</button>
              <span class="gallery-counter" id="galleryCounter">1 / 2</span>
              <button class="gallery-btn" id="galleryNext" aria-label="Next">Next &#8594;</button>
            </div>
          </div>
          <!-- Lightbox -->
          <div class="lightbox" id="lightbox" role="dialog" aria-modal="true" aria-label="Image viewer">
            <div class="lightbox-backdrop" id="lightboxBackdrop"></div>
            <button class="lightbox-close" id="lightboxClose" aria-label="Close">&#10005;</button>
            <div class="lightbox-inner">
              <div class="lightbox-zoom-controls">
                <button class="lightbox-zoom-btn" id="lightboxZoomOut" aria-label="Zoom out">&#8722;</button>
                <button class="lightbox-zoom-btn" id="lightboxZoomIn" aria-label="Zoom in">&#43;</button>
              </div>
              <div class="lightbox-img-wrap">
                <img class="lightbox-img" id="lightboxImg" src="" alt="" />
              </div>
            </div>
          </div>
        </div>
      </section>

      <section class="case-section">
        <h2 class="case-section-title">4. Outcome &amp; Lessons</h2>
        <div class="case-field">
          <h3 class="case-field-label">KPIs</h3>
          <p class="case-field-value">Did you meet your success metrics? Use a simple table or bullet list.</p>
        </div>
        <div class="case-field">
          <h3 class="case-field-label">Post-Launch</h3>
          <p class="case-field-value">What did you learn from real-world usage?</p>
        </div>
        <div class="case-field">
          <h3 class="case-field-label">Next Steps</h3>
          <p class="case-field-value">How would you iterate on this in &ldquo;Phase 2&rdquo;?</p>
        </div>
      </section>

    </div>
  </main>
</Layout>

<style>
  .case-study-body { max-width: 860px; margin: 0 auto; padding: 64px 24px 96px; color: #fff; }
  .case-section { margin-bottom: 64px; border-top: 1px solid rgba(255,255,255,0.08); padding-top: 48px; }
  .case-section-title { font-size: 26px; font-weight: 700; color: rgb(255,184,0); margin-bottom: 36px; }
  .case-section-sub { font-size: 18px; font-weight: 400; color: rgba(255,184,0,0.6); }
  .case-field { margin-bottom: 32px; }
  .case-field-label { font-size: 11px; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: rgba(255,255,255,0.4); margin-bottom: 8px; }
  .case-field-value { font-size: 17px; line-height: 1.75; color: #fafafa; font-style: italic; }
  /* Gallery */
  .gallery-wrap { margin-top: 20px; }
  .gallery-track-wrap { overflow: hidden; border-radius: 12px; background: rgba(0,0,0,0.25); }
  .gallery-track { display: flex; transition: transform 0.4s ease; }
  .gallery-img { flex: 0 0 100%; width: 100%; max-height: 480px; object-fit: contain; cursor: zoom-in; display: block; }
  .gallery-controls { display: flex; align-items: center; justify-content: space-between; margin-top: 14px; }
  .gallery-btn { background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.15); color: #fafafa; padding: 8px 20px; border-radius: 8px; font-size: 14px; cursor: pointer; transition: background 0.2s; }
  .gallery-btn:hover { background: rgba(255,255,255,0.16); }
  .gallery-counter { font-size: 13px; color: rgba(255,255,255,0.45); letter-spacing: 0.08em; }
  /* Lightbox */
  .lightbox { display: none; position: fixed; inset: 0; z-index: 9999; overflow-y: auto; }
  .lightbox.active { display: block; }
  .lightbox-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,0.88); z-index: 0; }
  .lightbox-close { position: fixed; top: 16px; right: 20px; z-index: 2; background: rgba(255,255,255,0.15); border: none; color: #fff; width: 42px; height: 42px; border-radius: 50%; font-size: 18px; cursor: pointer; display: flex; align-items: center; justify-content: center; transition: background 0.2s; }
  .lightbox-close:hover { background: rgba(255,255,255,0.3); }
  .lightbox-inner { position: relative; z-index: 1; display: flex; flex-direction: column; align-items: center; margin: 64px auto 40px; width: 100%; max-width: 92vw; }
  .lightbox-zoom-controls { display: flex; gap: 10px; margin-bottom: 14px; }
  .lightbox-zoom-btn { background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2); color: #fff; width: 38px; height: 38px; border-radius: 8px; font-size: 22px; cursor: pointer; display: flex; align-items: center; justify-content: center; transition: background 0.2s; line-height: 1; }
  .lightbox-zoom-btn:hover { background: rgba(255,255,255,0.2); }
  .lightbox-img-wrap { display: flex; align-items: flex-start; justify-content: center; border-radius: 8px; width: 100%; }
  .lightbox-img { display: block; width: 92vw; height: auto; border-radius: 8px; object-fit: contain; transition: width 0.2s ease; }
  .reveal-hidden { opacity: 0; transform: translateY(40px); transition: opacity 0.7s ease, transform 0.7s ease; }
  .reveal-visible { opacity: 1; transform: translateY(0); }
  .scroll-cue { position: absolute; bottom: 32px; left: 50%; transform: translateX(-50%); color: rgba(255,184,0,0.55); animation: cue-bounce 2s ease-in-out infinite; pointer-events: none; }
  @keyframes cue-bounce { 0%, 100% { transform: translateX(-50%) translateY(0); } 50% { transform: translateX(-50%) translateY(10px); } }
</style>

<script>
  // Scroll reveal
  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('reveal-visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.08 });
  document.querySelectorAll('.reveal-hidden').forEach(el => observer.observe(el));

  // Gallery
  const imgs = document.querySelectorAll<HTMLImageElement>('.gallery-img');
  const track = document.getElementById('galleryTrack') as HTMLElement;
  const counter = document.getElementById('galleryCounter') as HTMLElement;
  let current = 0;
  const total = imgs.length;

  function goTo(index: number) {
    current = (index + total) % total;
    (track as HTMLElement).style.transform = `translateX(-${current * 100}%)`;
    counter.textContent = `${current + 1} / ${total}`;
  }

  document.getElementById('galleryPrev')?.addEventListener('click', () => goTo(current - 1));
  document.getElementById('galleryNext')?.addEventListener('click', () => goTo(current + 1));

  // Lightbox
  const lightbox = document.getElementById('lightbox') as HTMLElement;
  const lightboxImg = document.getElementById('lightboxImg') as HTMLImageElement;
  let zoomLevel = 1;

  function openLightbox(src: string, alt: string) {
    lightboxImg.src = src;
    lightboxImg.alt = alt;
    zoomLevel = 1;
    lightboxImg.style.width = '92vw';
    lightboxImg.style.height = 'auto';
    lightbox.classList.add('active');
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox() {
    lightbox.classList.remove('active');
    document.body.style.overflow = '';
  }

  imgs.forEach(img => {
    img.addEventListener('click', () => openLightbox(img.src, img.alt));
  });

  document.getElementById('lightboxClose')?.addEventListener('click', closeLightbox);
  document.getElementById('lightboxBackdrop')?.addEventListener('click', closeLightbox);

  document.getElementById('lightboxZoomIn')?.addEventListener('click', () => {
    zoomLevel = Math.min(zoomLevel + 0.25, 4);
    lightboxImg.style.width = `${92 * zoomLevel}vw`;
    lightboxImg.style.height = 'auto';
  });

  document.getElementById('lightboxZoomOut')?.addEventListener('click', () => {
    zoomLevel = Math.max(zoomLevel - 0.25, 0.5);
    lightboxImg.style.width = `${92 * zoomLevel}vw`;
    lightboxImg.style.height = 'auto';
  });

  document.addEventListener('keydown', (e: KeyboardEvent) => {
    if (!lightbox.classList.contains('active')) return;
    if (e.key === 'Escape') closeLightbox();
  });
</script>
